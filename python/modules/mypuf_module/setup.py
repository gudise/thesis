from setuptools import setup

setup(name='mypuf',
      version='1.0',
      description='Módulo de Python para el proyecto puf.',
      author='gds',
      packages=['mypuf'],
      zip_safe=False)
